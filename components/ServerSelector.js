import React, { useState, useEffect } from 'react';
import { View, Text, Picker, Button } from 'react-native';
import { findServersOnLocalNetwork, login } from '../utils';

export default function ServerSelector(props) {
  return (
  );
}
